package com.acme.common.rest.model;

import javax.persistence.MappedSuperclass;

/**
 * The Base Entity that all other entities have to extend from.
 */
@MappedSuperclass
public abstract class BaseEntity {

}